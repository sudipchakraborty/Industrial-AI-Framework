from app.embeddings.embedding_router import (
    embedding_route
)

print(
    embedding_route(
        "need physician"
    )
)

print(
    embedding_route(
        "reserve accommodation"
    )
)

print(
    embedding_route(
        "turn on lamp"
    )
)